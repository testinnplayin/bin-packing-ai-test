{
  "name": "bin-packing-demo",
  "version": "1.0.0",
  "description": "Demo of three bin-packing algorithms",
  "keywords": [
    "bin-packing",
    "algorithms",
    "ai"
  ],
  "license": "ISC",
  "author": "R Wood, Cursor",
  "type": "commonjs",
  "scripts": {
    "test": "npm run test --prefix back",
    "test:watch": "npm run test:watch --prefix back",
    "test:coverage": "npm run test:coverage --prefix back",
    "build": "npm run build:client && npm run build:server",
    "build:client": "npm run build --prefix front",
    "build:server": "npm run build --prefix back",
    "start": "npm run start --prefix back",
    "dev:server": "npm run dev --prefix back",
    "dev:client": "npm run dev --prefix front",
    "watch": "npm run watch --prefix back"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
